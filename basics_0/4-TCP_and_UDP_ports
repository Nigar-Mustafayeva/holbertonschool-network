#!/usr/bin/env bash
# 4-TCP_and_UDP_ports
# Displays all listening TCP/UDP and UNIX domain sockets with PID/program name

echo "Active Internet connections (only servers)"
# TCP/UDP listening sockets with PID/program
ss -tulnp

echo "Active UNIX domain sockets (only servers)"
# UNIX domain listening sockets with PID/program
ss -xlnp
